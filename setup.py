from setuptools import setup


setup(
    name='jwallet-updates',
    version='0.1',
    description='jWallet updates management backend service',
    packages=[
        'jwallet_updates',
    ],
    zip_safe=False,
    platforms='any',
    install_requires=[],
    include_package_data=True,
)
